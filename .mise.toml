[env]
PULUMI_ROOT = "{{env.PWD}}/.root"

_.path = [
  "{{env.PULUMI_ROOT}}/bin",
]

[tools]
changie = "latest"
cue = "v0.12.0"
go = "1.23"
golangci-lint = "1.64.2"
jq = "latest"
"ubi:pulumi/pulumictl" = "latest"

[plugins]
