[env]
PULUMI_ROOT = "{{env.PWD}}/.root"

_.path = [
  "{{env.PULUMI_ROOT}}/bin",
]

[tools]
go = "1.23"
golangci-lint = "latest"
jq = "latest"
"ubi:cue-lang/cue" = "v0.12.0"
"ubi:pulumi/pulumictl" = "v0.0.48"

[plugins]
