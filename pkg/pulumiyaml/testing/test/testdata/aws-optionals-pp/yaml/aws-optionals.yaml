resources:
    example:
        type: aws:iam:Policy
        properties:
            name: example_policy
            path: /
            policy: ${policyDocument}
variables:
    policyDocument:
        Fn::Invoke:
            Function: aws:iam:getPolicyDocument
            Arguments:
                statements:
                    - actions:
                        - s3:ListAllMyBuckets
                        - s3:GetBucketLocation
                      resources:
                        - arn:aws:s3:::*
                      sid: "1"
            Return: json
