name: resource-secret
runtime:
  name: yaml
config:
  props:
    type: object
resources:
  randomPassword:
    type: random:RandomPassword
    properties: ${props}
outputs:
  superSecret:
    fn::secret: ${randomPassword}
