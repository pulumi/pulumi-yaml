name: aliasplugin
runtime: yaml
components:
  withChild:
    resources:
      child:
        type: random:RandomString
        properties:
          length: 8
    outputs:
      childResult: ${child.result}
