resources:
  explicit:
    type: pulumi:providers:component
  list:
    type: component:ComponentCallable
    properties:
      value: value
    options:
      providers:
        - ${explicit}
  map:
    type: component:ComponentCallable
    properties:
      value: value
    options:
      providers:
        - ${explicit}
