resources:
  elem:
    type: reservednames:ElementType
    properties:
      elementType:
        elementType: nested
outputs:
  elementType: ${elem.elementType}
  nested: ${elem.elementType.elementType}
